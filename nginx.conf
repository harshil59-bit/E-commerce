events {}

http {
    server {
        listen 80;

        # Frontend
        location / {
            proxy_pass http://frontend:80;
        }

        # Backend (FastAPI)
        location /api/ {
            proxy_pass http://backend:8000/;
        }
    }
}