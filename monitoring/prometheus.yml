global:
  scrape_interval: 5s

scrape_configs:
  # 🔹 EC2 system metrics
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']
  # 🔹 FastAPI backend metrics
  - job_name: 'fastapi-backend'
    static_configs:
      - targets: ['backend:8000']